
import { hasSpeakerInfo } from 'emberconf-2017/helpers/has-speaker-info';
import { module, test } from 'qunit';

module('Unit | Helper | has speaker info');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hasSpeakerInfo([42]);
  assert.ok(result);
});

